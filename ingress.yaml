apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vision-go-ingress
  annotations:
    kubernetes.io/ingress.class: "gce"
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vision-go-service
                port:
                  number: 80